name: Trigger Netlify Build

on:
  schedule:
    - cron: '0 9 * * *' # This will run the action every day at 09:00 UTC
  workflow_dispatch: # This allows the workflow to be run manually

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Trigger Netlify Build
      run: |
        curl -X POST -d {"branch":"main"} https://api.netlify.com/build_hooks/${{ secrets.NETLIFY_DAILY_BUILD }}
